ORG 3000H
RUTF10: IN AL, PB
        CMP AL, 11111111B
        JZ APAGAR
        MOV AL, 11111111B
        OUT PB, AL  
        JMP FIN
APAGAR: MOV AL, 0
        OUT PB, AL
FIN:    MOV AL, 20
        OUT EOI, AL
        IRET


ORG 2000H
MOV BX, 16
MOV AX, RUTF10
MOV [BX], AX
MOV AL, 0
OUT CB, AL
CLI
MOV AL, 11111110B
OUT IMR, AL

MOV AL, IDF10
OUT INT0, AL
STI

LOOP: JMP LOOP

INT 0
END
